# Public site URL used for Stripe Checkout redirects.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Stripe server secret. Do not expose this in client code.
STRIPE_SECRET_KEY=sk_test_replace_me

# SMTP credentials used by /api/contact.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=replace@example.com
SMTP_PASS=replace-with-app-password
SMTP_FROM="YB Visuals <replace@example.com>"
